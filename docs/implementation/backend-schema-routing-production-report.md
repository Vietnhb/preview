# Backend schema routing and physics implementation report

**Evidence refreshed:** 2026-09-21.

**Status: NOT PRODUCTION READY.** The implementation advances schema routing, candidate-bounded extraction, catalog integrity, typed output/end-condition contracts, and observability. Full solver/reference migration and all-model output-contract integration remain incomplete. Flyway V14 repairs the observed partial Hibernate schema additively, V15 adds multilingual semantic-view generations, and V16-V19 repair/normalize UTF-8 schema definitions and checksums. The configured Supabase database is now at V19. Broad production acceptance remains open; the checklist does not relax the source prompt.

## 0A. MVP multilingual routing checkpoint (2026-09-21)

**MVP MULTILINGUAL ROUTING NOT READY.** This checkpoint covers only CIRCUITS, DYNAMICS, and KINEMATICS. The implementation adds retrieval projection v2, reviewed source-controlled localized metadata, one bounded English and one bounded Vietnamese semantic view for all 29 latest MVP identities, accent-folded lexical shadow tokens, global stop words, grouped semantic-view storage, and additive Flyway V15 generation tables. Non-MVP documents retain the existing v1 projection path.

Before-change reproduction used the deterministic catalog-only projection equivalent of the previous runtime document (schema identity, English name/model, and contract keys/aliases/units; no localized metadata). It was run with `node scripts/evaluate-mvp-multilingual-retrieval.mjs --baseline` and is not a live-provider or database measurement. The six required smoke results were: Vietnamese RC discharge no lexical hit; Vietnamese Hooke rank 2; Vietnamese projectile no lexical hit; English RC discharge no hit in top 20; English Hooke no hit in top 20; English projectile rank 1. This confirms the reported multilingual lexical-recall gap without contacting configured infrastructure.

After-change deterministic offline evidence was run with `node scripts/evaluate-mvp-multilingual-retrieval.mjs --check`: 29-schema MVP corpus; 93 calibration cases; 29 held-out cases; 2 challenge cases; candidate recall@20 100%; candidate recall@5 100%; confident-selection precision 100% (synthetic surrogate); confident coverage 78.3% (below the required 80% gate); zero challenge guesses; all six smoke cases selected the expected identity at candidate rank 1. Per-topic held-out recall@5 was 100% for CIRCUITS (12 cases), DYNAMICS (11), and KINEMATICS (6). Wilson confidence intervals and real-provider embedding quality are not established by this surrogate evaluator.

The real disposable PostgreSQL/pgvector migration command was attempted as `cd backend; $env:DOCKER_HOST='npipe:////./pipe/dockerDesktopLinuxEngine'; .\mvnw.cmd -q '-Dtest=PgVectorSchemaSearchMigrationTest' test`. It was blocked before container startup because no valid Docker environment was available. A subsequent cloud catalog/bootstrap checkpoint is recorded in section 0B; the external embedding quality gate remains blocked because configured provider credentials/evidence were not used.

The latency gate is also **BLOCKED_EXTERNAL_INFRASTRUCTURE**: no honest P50/P95/P99 stage or total measurement can be reported without the disposable PostgreSQL/pgvector path and configured multilingual embedding provider. The offline evaluator is deliberately not used as a warmed-provider latency claim.

The extraction boundary remains bounded by `SchemaRoutingProperties.candidateTopK` (default 5): `OpenRouterExtractionProvider` serializes only the routed candidate contracts, validates current identity membership before and after each attempt, and retries with the same pinned messages. No schema-specific router branch or benchmark phrase was added; all multilingual phrases live in `schemas/retrieval/multilingual-metadata-v2.json` and are consumed generically by `SchemaRetrievalMetadataCatalog` and `SchemaSearchDocumentBuilder`.

When both retrievers return no rows, `SchemaRoutingService` now pins only the globally configured top-K approved contracts and returns `AMBIGUOUS/NO_RETRIEVAL_EVIDENCE`; it never converts a miss into an unbounded catalog prompt.

Focused implementation verification passed with `cd backend; .\mvnw.cmd -q '-Dtest=SchemaRoutingServiceTest,SchemaContractRerankerTest,UnicodePhysicsTokenizerTest,Bm25SchemaRetrieverTest,SchemaRetrievalMetadataCatalogTest,SchemaSearchDocumentBuilderTest,SchemaEmbeddingIndexerTest' test` and `cd backend; .\mvnw.cmd -q -DskipTests package`. The focused suite includes unit-only ambiguity, Vietnamese composed/decomposed and accent-folded tokens, OCR whitespace, decimal/unit/symbol tokens, global stop words, BM25 hand-calculation compatibility, metadata completeness, v2 projection construction, and indexer failure invalidation.

## 0B. Cloud catalog/bootstrap and UTF-8 checkpoint (2026-09-21)

The configured Supabase PostgreSQL pooler was reached through the repository's normal Spring configuration. The controlled command set `FLYWAY_ENABLED=true`, `BOOTSTRAP_CATALOGS_ENABLED=true`, and `PHYSLIVE_SCHEMA_ROUTING_ENABLED=false` before `cd backend; .\mvnw.cmd -q spring-boot:run`. Schema-routing was disabled for this run so no external embedding call or semantic reindex was attempted.

Flyway applied `V16__repair_schema_definition_utf8.sql`, `V17__restore_schema_definition_model_after_utf8_repair.sql`, `V18__repair_remaining_schema_definitions_utf8.sql`, and `V19__repair_remaining_mojibake_definitions.sql` successfully. The final cloud readback reported `schema_versions`: 182 `APPROVED`, 28 `RETIRED`, 210 total rows, and 74 distinct schema IDs; `definition_checksum IS NULL` = 0; definitions missing the catalog-injected `model` field = 0; and the backend bootstrap completed without a structural definition-drift failure. A direct canonical comparison using the same `SchemaCatalogIntegrity` logic returned 210/210 matching definitions. The UTF-8 marker query returned 0 definitions. These counts exactly match the 182 active catalog version rows and 28 historical published-version rows in source control. No destructive SQL was used. The temporary backend process was stopped after readback; the committed cloud migrations and catalog rows remain.

This proves the full source catalog is present, lifecycle-approved, UTF-8 repaired, structurally aligned with the checked-in catalog, and checksum-complete in the cloud database. It does not prove that multilingual vectors exist: the semantic-view row count remains 0 because this checkpoint intentionally did not run provider embedding/reindex.

## 0. Latest startup verification

On 2026-09-21, from `backend`, the controlled command with Flyway and catalog bootstrap explicitly enabled completed the V19 migration and catalog initialization against the configured PostgreSQL/Supabase database. The process reached the Tomcat listener on port 8080, and the final cloud readback showed zero null definition checksums and zero UTF-8 marker matches. The prior integrity failure was corrected without disabling integrity checks: schema and solver checksums use deterministic JSONB-safe canonicalization, and checksum metadata is repaired only after structural equality with the approved catalog is established. The Flyway PostgreSQL 17.6 support warning remains informational because this Flyway build officially supports PostgreSQL 16.

Targeted verification after the checksum change: `./mvnw.cmd -q -Dtest=SchemaCatalogIntegrityTest test` passed. The deployable artifact command `./mvnw.cmd -q package -DskipTests` passed and `demo-0.0.1-SNAPSHOT.jar` contains `V14__repair_partial_hibernate_schema.sql`. A full clean test has not been rerun after this latest checksum compatibility change; the older full-suite result is not reused as final evidence.

## 1. Baseline and current inventory

Initial `git status --short` showed no pre-existing repository changes other than the user-provided prompt file. No `AGENTS.md` was present. Baseline commands were run before source edits.

| Measure | Baseline | Current checkpoint |
|---|---:|---:|
| Catalog source-version rows / current schema IDs / topics / models | 74 / 74 / 9 / 74 | 182 / 74 / 9 / 74; 108 superseded source rows and 28 older published rows remain available for history/replay |
| Unique numerical/reference IDs in current routed schemas | 50 / 50 | 74 / 74 |
| Shared numerical/reference IDs in current routed schemas | 9 pairs; `applications_*` bound to 8 models across 4 topics | 0 shared pairs among latest routed versions; historical `applications_*` bindings remain shared across 7 models and 4 topics |
| Parameter `from(JsonNode, Map...)` binders | 63 | 0 in current `physics/model`; 59 retained under `physics/compatibility/legacy/model` |
| `PhysicsValues.require/optional` production references | 302 | 302, all under explicit compatibility packages |
| Solver/reference files mentioning `JsonNode` | 110 | 0 in current runtime packages; 110 retained under explicit compatibility packages |
| Model/schema dispatch switches in solver/reference packages | 10 | 0 in current runtime packages; 10 retained under explicit compatibility packages |
| Approved schema catalog serialized to extraction AI | About 108,000 characters / 27,000 estimated tokens | Removed from the extraction provider; max prompt characters 30,000 and candidate top-K 5 |
| Quantity definitions / aliases in current schema versions | 324 / 382 | Latest versions contain 320 quantity definitions and 385 aliases |
| Schema quantity defaults | 11 in the initial 74-row catalog | 36 defaults are declared in current schema versions; parameter-level fallbacks remain on legacy paths |
| Typed per-output declarations | 0 of 74 latest schema versions | 74 of 74 latest versions declare 290 typed outputs (243 time-series, 40 scalar, 7 scalar-field); the bound-module typed frame boundary is active, with native typed returns in two modules and an explicit legacy container adapter for the remaining modules |
| Catalog mojibake markers | 146 | All 182 source rows pass the versioned text check; older published rows remain preserved in the history archive |

The numerical/reference counts were remeasured after implementation. All 74 latest routed schema identities have typed module bindings with unique numerical/reference pairs. The current source contains 108 prior-version rows across those identities; 28 additional older published rows are stored in the separate history archive. Legacy raw solver/reference code is now physically isolated under `physics/compatibility/legacy/{solver,reference}`; it remains retained for historical replay and is not part of the current runtime packages.

## 2. Implemented changes

### Routing and extraction

- Added immutable search-document, candidate, retrieval-score, and decision models under `backend/src/main/java/com/example/backend/schema/routing/`.
- Search documents are deterministic typed projections of approved schema/curriculum metadata: description and learning outcomes, canonical keys, aliases, symbols, allowed units, relation types, end-condition capabilities, curriculum labels, and source checksum. Formula, solver/reference ID, output, and visualization data are excluded. Curriculum metadata coverage remains incomplete in source data.
- Added Unicode physics tokenization, BM25 with term/document frequencies and length normalization, exact pgvector cosine retrieval, deterministic RRF, generic contract evidence, configured confidence/margin policy, and explicit ambiguity results.
- Added typed `physlive.schema-routing.*` configuration. The current defaults use lexical/vector top-K 20, candidate top-K 5, RRF `k=60`, prompt cap 30,000 characters, query cap 20,000 characters, and embedding model `openai/text-embedding-3-small` at dimension 1536.
- Added an embedding provider abstraction and OpenRouter implementation. The pgvector store checks provider/model/dimension/source checksum and current search text, and receives the exact version-pinned identity set from the approved in-memory snapshot so stale approved embeddings cannot consume top-K slots. Exact cosine search is used; no ANN index is created.
- Added a bounded embedding transport policy: at most five configured attempts (default two), retrying only rate-limit, server, and transport failures with a bounded backoff window. Malformed vectors, non-retryable client errors, and wrong-model/dimension responses fail closed; no lexical-only or random fallback is used.
- Administrative index rebuilds now classify provider/transport failures as the typed `EmbeddingUnavailableException` (HTTP 503) and reject disabled routing through the same actionable availability path; schema compilation and dimension-contract failures remain distinct configuration defects.
- The extraction provider now uses `ExtractionPromptBuilder` and candidate contract projections. Strict duplicate-key and trailing-token parsing, candidate/version/quantity membership, and unit-catalog plus candidate-allowed-unit checks happen before POJO binding. A `SELECTED` route requires the exact backend-selected schema/version; an `AMBIGUOUS` route accepts only identities in its pinned set. Current approved identity is checked before the prompt and again after the AI response. Retry reuses the original pinned messages and appends a fixed short repair instruction; ambiguity resolution uses the current pinned approved version.
- The generic reranker recognizes compound unit tokens, explicit quantity-to-value connectors, configured numeric/domain contradictions, and declared `sameUnitAs` relations when both quantities have one unambiguous observed value. Ambiguous, multiple, or distant observations remain unsupported.
- RRF now treats each retriever ranking as an identity ranking: repeated schema/version identities are ignored after their best-ranked occurrence, while rank order is still validated. This follows the required `sum(1 / (k + rank))` identity-deduplication rule and keeps provider duplicates from becoming false routing outages.
- Added routing health/readiness checks and bounded metrics without problem text labels. Readiness compares the index identities with the active approved schema identities. A failed index rebuild invalidates the previous snapshot so a stale retired schema cannot remain routable. Legacy specification adapter invocations and prompt character count are measured without storing raw AI output.
- Corrected the selected-rank metric to record the best positive lexical/vector rank for the chosen candidate; it no longer reports a constant `1` when the candidate was returned lower or by only one retriever.
- Routing failure classification is now bounded by responsibility: embedding/provider and pgvector retrieval failures remain `EmbeddingUnavailableException`, while candidate fusion/verification defects remain `SchemaRoutingException` and retain their cause. Duplicate identities are now deduplicated by RRF at the best rank; `SchemaRoutingServiceTest.repeatedVectorIdentityIsDeduplicatedBeforeCandidateVerification` covers the provider-duplicate case.
- Added the versioned retrieval fixture and offline evaluator at `docs/evaluation/schema-routing-retrieval-fixture.json` and `scripts/evaluate-schema-routing-retrieval.mjs`. It covers all 74 active identities, separates 59 calibration and 15 held-out answerable cases, includes five synthetic challenge cases, and reports BM25, deterministic fake-embedding cosine, RRF, contract evidence, confidence, coverage, Wilson intervals, and per-topic counts. The offline thresholds pass (recall 100%, confident precision 100%, coverage 86.7%, zero challenge guesses); the real provider quality gate remains explicitly `BLOCKED_EXTERNAL_PROVIDER` until configured-provider evidence is collected.

### Catalog, persistence, and compatibility

- Split the schema source into topic-scoped modules under `backend/src/main/resources/schemas/source/`; added deterministic generation and drift checking with `scripts/generate-schema-catalog.mjs`. The latest sound-wave contract moves phase, domain, probe, and sample defaults into compiled schema data. Added 44 new versioned schema source rows for the previously structural-only output contracts; their predecessor definitions remain unchanged for replay.
- Added `scripts/repair-versioned-schema-text.mjs`. Text repairs create newer schema versions; existing persisted versions, runs, and assignment snapshots are not rewritten by the source repair. Typed migrations add dedicated versions and solver/reference pairs for diode characteristic, resistor networks, circular motion, adiabatic and isothermal and isobaric gas, thermal expansion, work-energy power, isochoric gas, RC charge/discharge, linear drag, uniform acceleration, and uniform electric field while retaining the prior published bindings.
- The shared unit catalog now declares the compound units used by catalog contracts, including `kg/s`, `kg/J`, `J/kg`, `1/m`, `rad/s`, `dB/m`, `N*m`, `Pa/s`, and `Pa/s2`. `SchemaCompiler` rejects unknown quantity units and typed output units; current and archived source-unit scans find no missing aliases.
- Preserved 28 older published catalog rows in `schemas/history/published-versions.json`. Bootstrap seeds missing archived schema/solver rows as retired before current approved entries; existing lifecycle values are not changed. Current candidate lookup requires the latest exact approved identity. Historical replay has a separate published-version resolver and a versioned legacy dimensional-suffix adapter; current lookups no longer strip `_1d`/`_2d` suffixes.
- Published schema initialization now checks stored name/topic and canonicalized stored definition against checked-in source even when a checksum already exists. Null checksums are backfilled only after structural equality is established.
- Added V8 pgvector storage, V9 `solver_versions.binding_checksum`, V10 `schema_search_embeddings.projection_version`, V11 typed simulation-run contract identity columns plus an indexed `(schema_id, schema_version, binding_version)` lookup, and V12 creation of missing `ambiguity_cases`/`reviewer_decisions` relations for legacy V11 databases. New runs pin schema/version, binding version and solver IDs, output-contract version/checksum, and replay rejects conflicting stored metadata; historical pre-V11 rows remain nullable and replay-compatible.
- V8 intentionally has no foreign key to `schema_versions`: Flyway runs before Hibernate creates its tables on fresh installs. The vector query joins embeddings to current approved schema rows and enabled topics and filters the current projection version.
- Added Testcontainers coverage for V8–V12 from V7/V11-baselined empty/prior fixtures, including pgvector, cosine distance, vector dimension checks, projection-version identity, preserved schema JSON, typed simulation-run contract identity columns/index, legacy ambiguity/reviewer table repair, stale search-text reindexing, and exclusion of an old approved vector identity. Both Testcontainers migration fixtures and the persisted replay fixture execute and pass with Testcontainers 1.21.4 against PostgreSQL 16.

### Runtime contracts and source quality

- Added sealed typed output variants (`ScalarOutput`, `TimeSeriesOutput`, `VectorSeriesOutput`, `ScalarFieldOutput`) and structural validation, with a versioned legacy serialization adapter. All 74 latest schema versions now declare per-output kind/unit/required contracts (290 outputs: 243 time-series, 40 scalar, and 7 scalar-field). `BoundPhysicsModule.solve(contract, clock)` now emits a typed `PhysicsOutputFrame` and `SimulationService` validates that frame before end-condition processing/persistence; `AcWaveformModule` and `KinematicsProjectileModule` now emit typed frames natively from shared numerical sample paths, while the remaining modules still use the explicit transitional adapter.
- Added `PhysicsOutputFrameMapper` as the single grouped-output transition boundary. It takes units only from the pinned compiled contract, rejects conflicting duplicate keys, and keeps the old grouped representation available only for legacy API/end-condition adapters.
- The compatibility projection now handles repeated legacy group projections deterministically: an identical key/shape/unit is emitted once, while conflicting duplicate data fails before persistence. Compiled visualization/source bindings map typed output keys to API groups generically, including output keys whose source basename differs (for example a velocity output bound to `velocities.x`). Two modules now use the native typed return path; full per-module migration remains open.
- Added `CanonicalQuantityCompiler` to require raw numeric values and original units, verify canonical keys, re-normalize rather than trust stored normalized echoes, materialize compiled defaults, apply bounded adjustments, and enforce integer/positive/non-negative/minimum/maximum constraints. `SimulationService` binds all 74 latest schema identities to typed modules on initial runs, adjustments, and previews. Historical and compatibility paths still use legacy JSON-based solver implementations.
- Production compiler calls now pass schema ID/version/topic from `SchemaVersion` and enforce the stored checksum; the two-argument inferred-identity overload remains for compatibility/tests. `CompiledSchema` now holds immutable end-condition output-source bindings; `TypedEndConditionCompiler` resolves sources once against that pinned contract, and `SimulationService` reuses the compiled condition through bounded horizon expansion. Historical solver paths use the explicit `LegacyEndConditionJsonAdapterV1`. Resource limits and declarative end-condition capabilities are not yet fully represented in compiled schemas.
- Added an immutable `PhysicsModuleRegistry` that rejects duplicate IDs and mismatched numerical/reference pairs. Its explicit registry currently contains 74 typed modules. New typed bindings use new versioned solver IDs while older schema versions keep their prior bindings.
- Moved the legacy raw JSON solver/reference trees and registries into `physics/compatibility/legacy/{solver,reference}` and updated 137 Java source/test files. Current runtime solver/reference package scans now contain zero `JsonNode` files and zero dispatch switches; 110 raw JSON files and 10 switches remain explicitly inside the versioned compatibility boundary for historical replay.
- Moved the 59 legacy-only `*Parameters` binders and the `PhysicsValues` bridge into `physics/compatibility/legacy/model` and `physics/compatibility/legacy`. Current `physics/model` has no `from(JsonNode, Map...)` binder and no `PhysicsValues` call; legacy solver/reference callers now make the compatibility bag conversion explicit. The four parameter records shared by typed modules retain only their canonical bag binder.
- Added `scripts/check-physics-runtime-boundary.mjs` and the CI `physics-boundary` job. The gate fails if raw JSON or dispatch switches re-enter current solver/reference/model packages, if old package imports remain, or if the explicit legacy entry point/package disappears; local result: `currentFiles=12, compatibilityFiles=114, compatibilityRawJson=110`, exit 0.
- Added `docs/physics/model-evidence-manifest.json`, generated from the latest catalog identities, plus `scripts/check-physics-evidence-manifest.mjs`. The structural gate rejects missing/stale identities, invalid evidence paths, and unverifiable `VERIFIED` entries. All 74 entries are currently `PARTIAL`: each has catalog-wide output and binding audit links, while model-specific golden, boundary/domain, invariant, independent-reference, and fault evidence remain empty. No independent evidence is fabricated by generation.
- Added typed schema-compilation, schema-routing, solver-binding, physics-domain, and output-contract exceptions with stable HTTP mappings. `SimulationService` preserves typed failures through preview and persistence catches instead of flattening server defects into 422 responses.
- Added typed v2 bindings for `light_interference@1.2` and `wave_pulse@1.2`, leaving v1.1 solver pairs intact for replay. Light interference accepts optional `path_difference_rate` (default `0 m/s`), exposes phase in radians, and has dynamic and signed-phase goldens. Wave-pulse probe rendering binds to the canonical `probe_position`; its tests cover Gaussian derivatives, advection, grid density, and resource bounds.
- Added typed v2 bindings for `wave_superposition@1.1` and `wave_reflection@1.1`, leaving v1.0 solver pairs intact for replay. Superposition has direct component-sum numerics and an independent phasor oracle. Reflection uses a schema-owned coefficient with a compiled `-1` default instead of interpreting boundary relations at runtime; wall-node/free-end goldens, oracle checks, unit/domain checks, sampling, and resource bounds are covered.
- Added an unconditional highest-priority startup audit over the active catalog: every latest schema identity must resolve through one typed numerical/reference module pair. `SimulationService.run()` now resolves the exact approved schema/version and fails before legacy registry lookup or persistence if a current pair is missing. `LegacyPhysicsExecutionAdapterV1` permits historical raw execution only for an exact schema/version/binding-version/solver-pair tuple present in superseded source rows or the archived published catalog; the same authorization guards the reference path.
- Added typed bindings for `kinematics_projectile@1.10`, `string_wave@1.2`, `magnetic_force@1.2`, `radiation_safety@1.1`, and `point_charge_field@1.1`; earlier published versions remain pinned for replay. Tests cover hand-calculated projectile and inverse-square cases, wave sampling and derivatives, magnetic near-parallel limits, independent reference formulations, domain boundaries, and resource bounds.
- `SolverOutput` now preserves first-class scalar outputs independently from time series. Scalar and scalar-field contracts are declared from the module output shape, while a versioned legacy adapter exposes a scalar as a one-element array only at the old `values` response boundary. Schema compilation verifies quantity and declared output units against the shared unit catalog and carries per-output absolute/relative tolerances and numeric/exact/discrete comparison into `PhysicsValidationService`. All 74 latest versions have declarations; two representative modules now return typed frames natively, while native migration of the remaining modules and full per-model runtime output evidence still need completion (ADR 0007).
- `PhysicsValidationService` now compiles the schema once per validation and rejects any reference key absent from the compiled output-key contract. Typed module runs use the dedicated `validateTyped(..., PhysicsOutputFrame, ...)` path, which reads scalar/time-series/vector probes from the immutable typed frame instead of reconstructing validation input from the grouped legacy container. `EndConditionResolver` now resolves and trims typed frames directly for the typed runtime path, preserving vector and scalar-field shapes; grouped `SolverOutput` overloads remain only for the explicit legacy/API compatibility path. The default tolerance is used only for a declared key without an explicit per-output tolerance; an oracle cannot introduce a new persisted output through the validation path.
- Replaced the audited request-path full-table filters with repository queries: Library search/community applies visibility, moderation, active state, and topic in JPA; school revenue uses one aggregate projection; finalized benchmark selection runs its adjudication/consensus rule in the database. PostgreSQL-backed tests cover the aggregate and finalization query behavior. Other administrative endpoints still expose unpaged list results.
- Replaced assignment replay's in-memory run scan with indexed repository lookups: latest run and the latest run at or before an assignment timestamp now use bounded `findFirst...OrderByCreatedAtDesc` queries. The persisted assignment replay integration verifies both selectors against PostgreSQL.
- Added typed end-condition contracts and a strategy registry for time limit, threshold, event, cycle count, and manual conditions. Typed module paths bind source quantities against compiled output/visualization declarations once per run; undeclared, ambiguous, and wrong-group sources fail before solving. Historical solver paths use the versioned V1 JSON adapter.
- `SimulationService` now validates the output a second time after end-condition trimming/interpolation, immediately before a run can be persisted or returned. The post-trim check covers the final timeline, required probes, duplicate keys, finite values, and scalar-field shape.
- Added `DatabaseSchemaModeValidator`: Flyway-enabled startup now fails closed unless Hibernate is set to `validate`. The local compose steady-state path uses `validate`; Hibernate mutation modes remain available only for the explicitly documented Flyway-disabled empty-database bootstrap.
- Added a Java source encoding scanner that checks all files under `backend/src/main/java` and `backend/src/test/java`, and self-tests common corruptions while allowing Vietnamese and Greek symbols.
- Added ADRs `docs/adr/0005-hybrid-schema-routing.md`, `docs/adr/0006-pgvector-schema-index.md`, and `docs/adr/0007-typed-scalar-output-contract.md` documenting decisions and open gaps.

## 3. Verification evidence

### Backend

| Command | Result |
|---|---|
| Baseline `cd backend && .\\mvnw test` | 68 tests, 0 failures/errors |
| Previous clean test `cd backend; $env:DOCKER_HOST='npipe:////./pipe/dockerDesktopLinuxEngine'; .\mvnw clean test` | `BUILD SUCCESS` (Maven exit 0); 481 tests, 0 failures/errors/skips before V11 snapshot identity changes. This evidence is retained as the pre-change baseline for the final clean run below. |
| Focused module/catalog/compiler selector | 80 tests, 0 failures/errors/skips; includes all added typed module batches and current catalog-to-module binding checks |
| Latest catalog output-contract completeness `cd backend; .\\mvnw.cmd -q '-Dtest=SchemaCatalogOutputContractTest' test` | `BUILD SUCCESS`; 1 test verifies all 74 latest identities compile with non-empty typed output definitions and 290 declared outputs |
| Focused candidate/lifecycle/replay selector | 28 tests, 0 failures/errors/skips; exact selected candidate, stale schema rejection, fail-closed index refresh, health identity, versioned suffix replay, and typed validation/replay tests |
| Current focused regression selector `-Dtest=GlobalExceptionHandlerTypedMappingsTest,SimulationServiceFailureMappingTest,SchemaCompilerIdentityTest,PhysicsModuleRegistryTest,OutputContractValidatorTest,SchemaRoutingServiceTest,PhysicsValidationServiceTypedModuleTest,SchemaSearchDocumentBuilderTest,SchemaContractRerankerTest,PgVectorSchemaLifecycleIndexIntegrationTest,SchemaIdentityResolutionTest` | 33 tests, 0 failures/errors/skips; typed HTTP mappings/catch preservation, document projection, reranking evidence, live pgvector lifecycle fail-closed behavior, replay pinning, and independent reference fault injection |
| Routing failure classification `cd backend; .\mvnw '-Dtest=SchemaRoutingServiceTest' test` | `BUILD SUCCESS` before the RRF identity-dedup correction; candidate-verification failures were kept distinct from embedding outages |
| Selected-rank metric regression `cd backend; .\mvnw.cmd -q '-Dtest=SchemaRoutingServiceTest' test` | `BUILD SUCCESS`; selected-rank telemetry now derives from positive lexical/vector rank evidence instead of a constant `1` |
| RRF duplicate-identity regression `cd backend; .\mvnw.cmd -q '-Dtest=ReciprocalRankFusionTest,SchemaRoutingServiceTest' test` | `BUILD SUCCESS`; 7 tests, 0 failures/errors/skips; repeated vector identity is deduplicated at its best rank and routing remains candidate-bounded |
| Strict extraction unit-membership regression `cd backend; .\mvnw.cmd -q -Dtest=StrictSpecificationValidatorTest test` | `BUILD SUCCESS`; 11 tests, 0 failures/errors/skips; unknown units and units outside the pinned candidate contract are rejected before POJO binding while catalog aliases such as `N / m` remain accepted |
| BM25/RRF specification fixtures `cd backend; .\mvnw.cmd -q '-Dtest=Bm25SchemaRetrieverTest,ReciprocalRankFusionTest' test` | `BUILD SUCCESS`; 11 tests, 0 failures/errors/skips; includes the exact hand-calculated BM25 and RRF fixtures from the production prompt, empty/zero-match behavior, deterministic tie-breaks, and per-retriever identity deduplication |
| Offline retrieval evaluation `node scripts/evaluate-schema-routing-retrieval.mjs --check` | Pass; 74-schema corpus, 59 calibration cases, 15 held-out answerable cases, 5 challenge cases; candidate recall@5 100%, confident-selection precision 100% (13 confident cases), unambiguous coverage 86.7%, zero challenge guesses. This is synthetic catalog-derived algorithm evidence; the real embedding-provider quality gate is explicitly blocked without provider credentials/evidence |
| Output duplicate projection regression `cd backend; .\mvnw.cmd -q '-Dtest=OutputContractValidatorTest,ReciprocalRankFusionTest,SchemaRoutingServiceTest' test` | `BUILD SUCCESS`; 13 tests, 0 failures/errors/skips; identical legacy group/value projections collapse and conflicting duplicate output keys fail closed |
| Typed execution regression `cd backend; .\mvnw.cmd -q '-Dtest=PhysicsValidationServiceTypedModuleTest,SimulationServiceTypedPathTest,SimulationServiceScalarPersistenceTest' test` | `BUILD SUCCESS`; 4 tests, 0 failures/errors/skips; one bound module emits a contract-shaped `PhysicsOutputFrame`, the service validates typed frames before persistence, independent typed validation/fault detection remain green, and scalar snapshot identity remains pinned |
| Typed end-condition/native output regression `cd backend; .\mvnw.cmd -q '-Dtest=EndConditionStrategyRegistryTest,TypedEndConditionCompilerTest,KinematicsProjectileModuleTest,PhysicsValidationServiceTypedModuleTest,SimulationServiceTypedPathTest' test` | `BUILD SUCCESS`; typed threshold resolution, compiled source-key/group projection, native projectile and AC waveform frames, vector trim, persisted typed simulation, and independent reference validation remain green |
| Typed output-source mapping regression `cd backend; .\mvnw.cmd -q '-Dtest=TypedEndConditionCompilerTest' test` | `BUILD SUCCESS`; 4 tests, 0 failures/errors/skips; a visualization output key such as `vx` resolves to the compiled grouped source `velocities.x` without name heuristics |
| Reference output-key contract `cd backend; .\mvnw.cmd -q '-Dtest=PhysicsValidationServiceTypedModuleTest,PhysicsValidationServiceFaultInjectionTest' test` | `BUILD SUCCESS`; 3 tests, 0 failures/errors/skips; typed references stay within compiled output keys and the independent projectile/fault oracle remains effective |
| Routing integration regression `cd backend; .\mvnw '-Dtest=SchemaRoutingServiceTest,SchemaRoutingSimulationPersistenceIntegrationTest,OpenRouterEmbeddingClientTest' test` | `BUILD SUCCESS`; routing, fake extraction-to-persistence, and embedding client tests pass; only JVM dynamic-agent warnings remain |
| Focused end-condition/compiler/compatibility selector `-Dtest=TypedEndConditionCompilerTest,EndConditionStrategyRegistryTest,SimulationServiceTypedPathTest,SimulationServiceScalarPersistenceTest,SchemaCompilerIdentityTest,SchemaCompilerOutputValidationTest,PhysicsValuesCanonicalTest` | 26 tests, 0 failures/errors/skips; end-condition source pinning and reuse, legacy adapter bridge, compiled schema validation, and scalar persistence |
| Current runtime-boundary selector `-Dtest=ApprovedLatestPhysicsModuleBindingAuditTest,LegacyPhysicsExecutionAdapterV1Test,SimulationServiceLegacyBoundaryTest,SimulationServiceTypedPathTest,SimulationServiceScalarPersistenceTest,PhysicsValidationServiceTypedModuleTest,PhysicsValidationServiceFaultInjectionTest,SchemaIdentityResolutionTest,SchemaRoutingSimulationPersistenceIntegrationTest` | 24 tests, 0 failures/errors/skips; active catalog latest-pair audit, exact historical permits, current suffix identity rejection, no current legacy fallback/persistence, permit-gated historical preview re-execution, typed execution, and routing-to-persistence |
| Fresh/upgrade migration plus typed snapshot identity `cd backend; $env:DOCKER_HOST='npipe:////./pipe/dockerDesktopLinuxEngine'; .\mvnw.cmd -q '-Dtest=FreshSchemaBootstrapMigrationTest,PgVectorSchemaSearchMigrationTest,HistoricalRunSnapshotReplayIntegrationTest,SimulationServiceTypedPathTest,SimulationServiceScalarPersistenceTest' test` | `BUILD SUCCESS`; PostgreSQL 16 fixtures applied V1–V12, pgvector cosine queries passed, V11 run identity columns/index and V12 ambiguity/reviewer tables were validated, persisted retired snapshot save/clear/reload/replay preserved pinned identity/content, and typed persistence selectors passed. |
| V6/V11 partial-bootstrap repair `cd backend; $env:DOCKER_HOST='npipe:////./pipe/dockerDesktopLinuxEngine'; .\mvnw.cmd -q '-Dtest=FreshSchemaBootstrapMigrationTest,PgVectorSchemaSearchMigrationTest' test` | `BUILD SUCCESS`; PostgreSQL 16 migration fixtures, 0 failures/errors/skips. A full Hibernate entity schema was reduced to the reported V6 state by removing only `schema_versions`; the Flyway callback recreated the entity table before V7, V7–V12 completed, Hibernate `validate` passed, and unrelated legacy data remained unchanged. A V6 database with an existing published schema row preserved its UUID, JSON definition, and null checksum. A V11 database missing `ambiguity_cases` and `reviewer_decisions` was repaired by V12. V7-baselined databases remained unaffected. |
| Replay identity guard `cd backend; $env:DOCKER_HOST='npipe:////./pipe/dockerDesktopLinuxEngine'; .\mvnw.cmd -q '-Dtest=SimulationServiceLegacyBoundaryTest,HistoricalRunSnapshotReplayIntegrationTest' test` | `BUILD SUCCESS`; 6 tests, 0 failures/errors/skips. A stored run whose pinned schema identity conflicts with its simulation is rejected; matching historical identity and assignment-pinned identity remain replayable through the persisted snapshot path. The focused legacy preview case re-executes only through an exact historical permit. |
| Historical definition integrity `cd backend; .\mvnw.cmd -q '-Dtest=SchemaIdentityResolutionTest' test` | `BUILD SUCCESS`; 7 tests, 0 failures/errors/skips. Historical lookup remains available after topic retirement without consulting enabled-topic routing, while a non-null stored definition checksum mismatch fails before replay. |
| Projection identity unit `cd backend; .\mvnw.cmd -q '-Dtest=SchemaSearchDocumentBuilderTest' test` | `BUILD SUCCESS`; deterministic search documents carry the current projection version |
| Concrete historical adapter replay `cd backend; .\mvnw.cmd -q '-Dtest=LegacyPhysicsExecutionAdapterV1Test' test` | `BUILD SUCCESS`; 6 tests, 0 failures/errors/skips. An exact historical kinematics permit executes the concrete legacy numerical solver and independent reference pair; latest routing remains rejected. Full service-level legacy re-execution remains open. |
| Assignment snapshot selection `cd backend; .\mvnw.cmd -q '-Dtest=AssignmentServiceTest' test` | `BUILD SUCCESS`; 11 tests, 0 failures/errors/skips. A student replay uses the assignment's pinned run ID; a pre-V11 assignment without one resolves the run at assignment time before replay. |
| Persisted assignment replay `cd backend; $env:DOCKER_HOST='npipe:////./pipe/dockerDesktopLinuxEngine'; .\mvnw.cmd -q '-Dtest=HistoricalRunSnapshotReplayIntegrationTest' test` | `BUILD SUCCESS`; 2 PostgreSQL/Testcontainers tests, 0 failures/errors/skips. A pinned assignment is saved, cleared, reloaded through `AssignmentRepository`, and replayed through `AssignmentService` → `SimulationService.replay` with the persisted run identity and output. |
| Embedding/provider failure policy `cd backend; .\mvnw.cmd -q '-Dtest=EmbeddingRetryPolicyTest,OpenRouterEmbeddingClientTest,EmbeddingResultTest' test` | `BUILD SUCCESS`; 9 tests, 0 failures/errors/skips; retry attempts are bounded, `Retry-After` is honored within a safe cap, only transient classes retry, zero-norm/overflowing-norm vectors are rejected, and malformed/non-retryable responses fail closed |
| Admin reindex HTTP contract `cd backend; .\mvnw.cmd -q '-Dtest=SchemaRoutingAdminControllerContractTest' test` | `BUILD SUCCESS`; 2 tests, 0 failures/errors/skips. A dry-run request is delegated and returned as the typed result; a missing `dryRun` field is rejected before the indexer is invoked. The live lifecycle test now proves provider failure invalidates readiness and raises the typed availability exception. Security, concurrent-reindex, and real HTTP-to-PostgreSQL endpoint integration coverage remain open. |
| Database schema mode guard `cd backend; .\mvnw.cmd -q '-Dtest=DatabaseSchemaModeValidatorTest' test` | `BUILD SUCCESS`; 3 tests, 0 failures/errors/skips. Flyway-enabled startup rejects Hibernate `update/create/create-drop`; the documented Flyway-disabled bootstrap modes remain valid. |
| Physics evidence manifest `node scripts/check-physics-evidence-manifest.mjs --check` | Pass; 74 latest catalog identities are represented deterministically; 0 verified, 74 partial, and 0 pending entries. Strict `--release` mode remains intentionally failing until independent per-model evidence is linked |
| Physics evidence release gate `node scripts/check-physics-evidence-manifest.mjs --release` | Exit 1 as required: `release evidence is incomplete: 0/74 identities are VERIFIED`; this is an explicit unresolved production blocker, not a skipped test |
| `node scripts/check-backend-source-encoding.mjs` | Pass |
| `node scripts/check-physics-runtime-boundary.mjs` | Pass; current solver/reference/model scan has 0 raw-JSON files and 0 dispatch switches; 110 raw-JSON files remain only in 114 explicit compatibility solver/reference Java files |
| `node scripts/generate-schema-catalog.mjs --check` | Pass; 182 entries |
| `node scripts/repair-versioned-schema-text.mjs --check` | Pass; 182 entries |
| Static scan: parameter binders `rg -n 'from\\(JsonNode' backend/src/main/java/com/example/backend/physics/model` | 0 current model binders; 59 legacy binders are under `physics/compatibility/legacy/model` |
| Static scan: current runtime `rg -l '\bJsonNode\b' backend/src/main/java/com/example/backend/physics/solver backend/src/main/java/com/example/backend/physics/reference` | 0 files; the old packages are absent after explicit compatibility isolation |
| Static scan: compatibility `rg -l '\bJsonNode\b' backend/src/main/java/com/example/backend/physics/compatibility/legacy/solver backend/src/main/java/com/example/backend/physics/compatibility/legacy/reference` | 110 files remain, all under the versioned compatibility boundary |
| Static scan: `rg -n 'PhysicsValues\.(require\|optional)' backend/src/main/java` | 302 production references remain |
| Static scan: current runtime `rg -n 'switch\s*\(' backend/src/main/java/com/example/backend/physics/solver backend/src/main/java/com/example/backend/physics/reference` | 0 dispatch switches |
| Static scan: compatibility `rg -n 'switch\s*\(' backend/src/main/java/com/example/backend/physics/compatibility/legacy/solver backend/src/main/java/com/example/backend/physics/compatibility/legacy/reference` | 10 switches remain, bounded to explicit historical compatibility |
| Static scan: `rg -n 'findAll\(' backend/src/main/java` | 8 calls remain; the audited Library search/community, school revenue aggregate, and finalized benchmark filters now use repository queries. Other administrative list endpoints remain unpaged |
| Static scan: current latest-per-schema catalog dimensional suffix check | 0 IDs end in public `_1d`, `_2d`, `-1d`, or `-2d` |
| Public schema/model/solver identity suffix scan over topic source files | No public dimensional `1d`/`2d` suffix found |
| Wildcard import scan over production Java | 28 existing wildcard imports found; no new wildcard import appears in tracked changes |
| `git diff --check` | Pass; Git prints LF-to-CRLF working-copy notices |

V8–V12 pass against live PostgreSQL 16 from both an empty database fixture and represented V7/V11 prior-schema fixtures. `FreshSchemaBootstrapMigrationTest` proves the existing V1–V12 chain succeeds for a verified empty database after Hibernate creates entity tables, Flyway baselines at version 0, and then applies all migrations; it also checks the V11 simulation-run identity columns/index and V12 reviewer tables. The same test covers the exact legacy failure where Flyway is at V6 and `schema_versions` is absent: a narrowly guarded `beforeMigrate` callback creates only the missing entity table, leaves existing rows untouched, and allows V7–V12 plus Hibernate validation to complete. A V11 fixture with missing ambiguity/reviewer relations is repaired by V12. `HistoricalRunSnapshotReplayIntegrationTest` proves a retired run's typed identity survives a real save/clear/reload and replay, while `SimulationServiceLegacyBoundaryTest` rejects a conflicting pinned identity. A normal one-pass Flyway startup directly against an empty schema still fails at V4 because `assignment_submissions` does not exist. The tested operator sequences are documented in `docs/implementation/fresh-database-bootstrap.md`. The test dependency is pinned to Testcontainers 1.21.4 because 1.19.8 is incompatible with this Docker Engine 29 environment. No already-applied migration was edited.

### Frontend and catalog coverage

| Command | Result |
|---|---|
| Baseline `cd react-client && npm run check` | Pass; 19 tests |
| Current `cd react-client && npm run check` | Pass; architecture, scene contract, lint, 23 tests, TypeScript, and Vite build |
| Scene contract check (part of `npm run check`) | 182 schemas; 92 explicit scene graphs; 90 series-driven scenes; 0 errors |
| `node scripts/check-curriculum-coverage.mjs` | 90/90 internal curriculum rows approved; 85/85 lessons mapped; no errors |
| `node scripts/report-topic-coverage.mjs` | 85/85 lessons mapped across 9 topics; 100% internal topic coverage |
| Official-program evidence | Not asserted; the 100% figure is internal catalog coverage |
| `git diff --check` | Pass; Git prints LF-to-CRLF working-copy notices |

Vite still warns about large emitted chunks: PDF worker 2395.54 KB, PixelBlast 546.99 KB, and main index 504.20 KB. Maven also reports existing deprecated API usage in `AcRlcParameters`, dynamic Byte Buddy agent-loading warnings, and PostgreSQL Testcontainers teardown connection warnings. Surefire logged its 30-second fork shutdown notice after the final tests had passed; the command still returned `BUILD SUCCESS`.

### Focused test evidence

- BM25: hand-calculated fixture, Unicode/Vietnamese/symbol tokens, document length handling, deterministic ranking.
- RRF: hand-calculated fixture and deterministic ties.
- Routing: fake vector retriever, confidence/margin ambiguity, pinned candidates, and metrics.
- Prompt/AI: bounded candidate-only projection, no solver/formula/visualization fields, duplicate-key rejection, response size/depth limits, strict shape, exact selected-candidate membership for `SELECTED`, pinned-set membership for `AMBIGUOUS`, canonical duplicate rejection through aliases, and relation/end-condition membership checks where declared. Current approved identity is rechecked before prompting and after the AI response.
- Typed outputs: scalar/time-series/vector/field structure and finite/shape checks. All 74 latest schemas have per-output declarations (290 outputs), 7 scalar-field definitions, and 40 scalar definitions; output-unit catalog verification/per-output tolerances are consumed by the typed frame validator. The bound runtime validates typed frames before end-condition processing and maps/validates the accepted post-trim frame again before persistence; two representative modules use native typed returns and compiled source-group projection, while the remaining module migration and per-model persistence evidence remain open.
- Typed runtime: 74 modules, including projectile motion, string waves, magnetic force, radiation safety, point-charge field, circuit/optics instruments, modern physics, dynamics graphs, radio, ultrasound, and practical data models; focused tests cover goldens, boundaries/domain checks, module registration, schema default materialization, independent references, and resource limits.
- Canonical ingress: raw value/unit normalization, default materialization, bounded adjustment, alias rejection past ingress, duplicate/malformed key rejection, and declared integer constraint checks.
- End conditions: time-limit/threshold/cycle/contact contracts bind against declared output sources; undeclared and wrong-group sources are rejected; `SimulationService` compiles once and reuses the condition across horizon expansion. The final trimmed/interpolated output is validated again before persistence; trim behavior covers timeseries/scalar fields.
- Catalog integrity: canonical JSON comparison, safe backfill, published metadata drift, and solver binding checksums.
- Physics evidence manifest: all 74 latest identities are present with explicit golden, boundary, invalid-domain, invariant, output, and fault-detection slots; the strict release check remains open because those slots are not yet independently populated.
- Catalog history: 28 superseded entries compile, remain separate from the active route catalog, and are seeded as retired before current approved entries in the bootstrap test.
- Fault injection: two test-only cases reject deliberately perturbed numerical output, including a typed projectile y-series shifted by +2 m against its independent reference. This is not a per-model mutation suite.

`SimulationServiceTypedPathTest` exercises a persisted typed simulation after a prepared specification exists. `SchemaRoutingSimulationPersistenceIntegrationTest` now composes real schema routing, candidate membership checks, raw-unit normalization at the extraction boundary, typed execution, reference validation, and a captured persisted run snapshot using a deterministic fake extractor.

## 4. Required checklist at this checkpoint

Status values: **DONE** means the complete checkbox is met; **PARTIAL** means relevant scaffolding exists but the prompt's full condition is not met; **NOT DONE** means implementation/evidence is absent; **BLOCKED** means the gate could not run in this environment.

### Schema routing

| Status | Requirement | Evidence / gap |
|---|---|---|
| DONE | AI prompt does not serialize the full approved catalog | `OpenRouterExtractionProvider`, `ExtractionPromptBuilderTest` |
| PARTIAL | Search document generated from approved schema/curriculum metadata | `SchemaSearchDocumentBuilder`; not every curriculum relation is represented in schema data |
| DONE | Real BM25 implementation, not keyword `contains` | `Bm25SchemaRetriever`, hand-calculated tests |
| PARTIAL | pgvector persists/retrieves correct model, dimension, projection version, checksum | `V8__add_hybrid_schema_search.sql`, `V10__add_schema_search_projection_version.sql`, `PgVectorSchemaEmbeddingStore`; PostgreSQL 16 integration tests pass; offline retrieval evaluation passes its deterministic algorithm gate, while real provider quality evidence and broader failure/reindex operations still need production evidence |
| DONE | Deterministic RRF | `ReciprocalRankFusionTest` |
| DONE | Generic contract reranker without schema/topic branches | `SchemaContractReranker` |
| DONE | Low confidence/margin becomes explicit ambiguity | `SchemaRoutingServiceTest` |
| PARTIAL | New schema routes after approval/index without router code changes | Startup rebuild, lifecycle refresh, topic-toggle refresh, and admin dry-run/reindex exist. `PgVectorSchemaLifecycleIndexIntegrationTest` checks live approved-version swaps, retired/draft/disabled exclusion, embedding-failure invalidation, and readiness; production admin reindex operations still need broader evidence |

### AI contract

| Status | Requirement | Evidence / gap |
|---|---|---|
| DONE | AI sees only bounded candidate contracts | top-K 5, prompt cap 30,000 characters, prompt tests |
| DONE | AI contract excludes visualization, solver implementation, formulas, and oracle values | `CandidateContractProjection`, `ExtractionPromptBuilderTest` |
| DONE | Backend rejects schemas/versions/units outside the candidate set | `StrictSpecificationValidator`; `SELECTED` exact identity, `AMBIGUOUS` pinned-set, and pre-binding unit-catalog/allowed-unit regression tests |
| DONE | Strict JSON shape and duplicate keys rejected before POJO binding | `StrictJsonParser`, strict validator tests |
| PARTIAL | Backend canonicalizes quantities and normalizes raw value/unit | extraction ingress does so; simulation solver APIs still receive JSON and aliases/legacy data |
| PARTIAL | Retry and ambiguity resolution retain pinned candidates/version | Retries reuse the pinned decision; ambiguity resolution requires a current pinned approved version. Full retry/replay integration matrix remains incomplete |

### Canonical physics runtime

| Status | Requirement | Evidence / gap |
|---|---|---|
| PARTIAL | Solver/reference runtime does not receive raw `JsonNode` | Current runtime solver/reference packages contain zero raw-JSON files after moving 110 files into `physics/compatibility/legacy/{solver,reference}`. Historical compatibility still retains those files; a permit-gated service preview now exercises the boundary, while real PostgreSQL full-service replay remains open |
| PARTIAL | One immutable canonical quantity bag compiled once per request | `CanonicalQuantityCompiler` compiles the latest typed request once; historical/compatibility solver paths retain `JsonNode` bridges and override maps |
| PARTIAL | Alias resolution only at ingress | Latest typed module paths receive exact canonical keys and reject aliases; 302 `PhysicsValues.require/optional` references remain only in explicit compatibility binders/implementations |
| PARTIAL | Defaults have one source in compiled schema | Latest typed paths materialize schema defaults; parameter-level fallbacks remain in explicit legacy compatibility binders |
| PARTIAL | Legacy data flows only through a versioned replay adapter | Numerical and reference raw solver calls are reachable from production services only through `LegacyPhysicsExecutionAdapterV1`, which checks exact historical identity and rejects latest active schemas. `LegacySchemaIdentityAdapter` and `LegacyEndConditionJsonAdapterV1` remain versioned. Adapter tests execute a concrete historical numerical/reference pair through an exact permit; `SimulationServiceLegacyBoundaryTest` now exercises service-level historical preview re-execution with an exact permit; persisted snapshot and assignment replay are covered, while real PostgreSQL full-service legacy re-execution remains open |
| PARTIAL | `PhysicsValues` absent from production path | All 302 references are physically isolated under explicit compatibility packages, and current typed modules no longer import the bridge. Snapshot/assignment replay and one service-level permit-gated preview are covered; complete legacy re-execution and removal of compatibility raw JSON remain open |

### Modules and academic independence

| Status | Requirement | Evidence / gap |
|---|---|---|
| PARTIAL | Immutable registry fails on duplicates/missing bindings | Typed registry rejects duplicate IDs and mismatched pairs; an unconditional startup audit checks all 74 latest active catalog identities. Historical raw permits and all persisted DB bindings do not yet have a complete startup audit |
| PARTIAL | New model requires no central model switch | Current runtime has zero solver/reference dispatch switches; 10 switches remain only in bounded historical compatibility. Dynamic onboarding and full compatibility permit audit are still incomplete |
| NOT DONE | No multi-topic solver/reference ownership | Latest routed IDs have unique pairs; historical `applications_*` bindings still span 7 models and 4 topics |
| NOT DONE | Reference formulas are independent of numerical path | Many legacy parameter records expose formulas shared by numerical/reference paths; independent reference coverage remains incomplete |
| PARTIAL | Every model has golden, invariant, boundary, and invalid-domain evidence | `docs/physics/model-evidence-manifest.json` now keys the required six evidence kinds to all 74 latest identities; all entries remain `PARTIAL` with only catalog-wide structural links until independent executable links are reviewed and populated |
| PARTIAL | Mutation/fault injection proves incorrect numerical output is detected | `PhysicsValidationServiceFaultInjectionTest` rejects one deliberately perturbed series against an independent test oracle; per-model mutation/fault evidence is absent |

### Typed outputs and end conditions

| Status | Requirement | Evidence / gap |
|---|---|---|
| PARTIAL | Domain output distinguishes scalar/series/vector/field | Sealed hierarchy and validator exist; all 74 latest schema versions declare kind/unit/required contracts (290 outputs), and `BoundPhysicsModule.solve(contract, clock)` emits a typed frame. `AcWaveformModule` and `KinematicsProjectileModule` now emit typed frames natively from a shared numerical sample path; the remaining 72 modules still use the explicit transitional projection, so full native per-module migration remains incomplete |
| PARTIAL | Key/kind/unit/shape/finiteness all validated before persist | Contract declarations cover all 74 latest schemas and compile against the unit catalog; the typed frame is validated before end-condition processing and the accepted post-trim frame is mapped and validated again before persistence. The API projection now uses compiled visualization/source bindings (including grouped position/velocity/acceleration mappings) instead of schema branches; 72 module implementations still require the transitional grouped adapter |
| DONE | Per-output absolute/relative/exact/discrete tolerance | `SchemaCompiler` compiles numeric/exact/discrete comparison and absolute/relative tolerances; `PhysicsValidationService` applies the compiled contract; compiler/fault-injection tests cover it |
| PARTIAL | Typed end-condition strategy and compiled output binding | Typed strategy registry, immutable compiled source bindings, typed-frame resolution/trimming, and per-run contract compilation exist for typed modules; historical paths use a versioned adapter and end-condition/resource capabilities are not fully compiled from catalog definitions |
| PARTIAL | Trim/replay preserves every typed output shape | Scalar-field/time-series trim tests plus post-trim contract validation and scalar persistence integration exist; historical full-shape replay regression remains absent |

### Catalog and persistence

| Status | Requirement | Evidence / gap |
|---|---|---|
| DONE | Topic/model source modules and deterministic generation | `schemas/source/`, `generate-schema-catalog.mjs --check` |
| PARTIAL | Schema and solver bindings have checksum drift protection | Schema definition/name/topic and solver ID/output binding are checked; V9 adds solver checksum, V10 versions the derived search projection, V11 pins simulation-run contract identity, and V12 repairs missing reviewer relations without rewriting data. Historical lookup now verifies a stored non-null schema checksum without requiring an enabled topic; PostgreSQL 16 V8–V12 integration tests pass; full schema bootstrap remains incomplete |
| DONE | Null checksum backfill verifies stored content before writing | `SchemaCatalogIntegrity`, tests |
| DONE | Published versions are not mutated by catalog bootstrap | Drift fails with instruction to create a new version; text repair uses new versions |
| PARTIAL | Historical run/assignment snapshots remain replayable | V8–V12 do not rewrite them. `V11__pin_simulation_run_contract_identity.sql` stores nullable typed identity for new runs while preserving pre-V11 rows; V12 only creates missing reviewer relations. `HistoricalRunSnapshotReplayIntegrationTest` persists a retired pinned snapshot in PostgreSQL, clears and reloads it through repositories, then replays the persisted result through `SimulationService` while preserving identity, values, time, validation, and end-condition content; its second test persists and reloads an assignment, then exercises `AssignmentService` → `SimulationService.replay` against the pinned run. `SimulationServiceLegacyBoundaryTest` rejects conflicting identity and exercises permit-gated legacy preview re-execution. `SchemaIdentityResolutionTest` verifies disabled-topic historical lookup and checksum drift rejection. `AssignmentServiceTest` covers the pre-V11 assigned-at fallback. Real PostgreSQL full-service legacy solver re-execution remains open |

### Quality and operations

| Status | Requirement | Evidence / gap |
|---|---|---|
| PARTIAL | Large services split by responsibility | Prompt building/routing separated; `ProblemService` and `SchemaDefinitionService` remain large |
| DONE | Request paths do not `findAll()` then filter in memory | The audited library visibility/topic filters, school revenue summary, and benchmark finalization now execute in repository queries; the eight remaining `findAll()` calls are unfiltered administrative lists |
| DONE | Typed exception taxonomy and HTTP mappings | Core schema compilation/routing, solver binding, physics-domain, output-contract, canonical-contract, and embedding failures have typed HTTP mappings; `SimulationServiceFailureMappingTest`, `GlobalExceptionHandlerTypedMappingsTest` |
| DONE | Mojibake is checked and repaired | The UTF-8 repair pass is clean across `schemas/**/*.json`; active, historical, and source definitions were repaired, and cloud readback reports zero mojibake markers with all 210 definition checksums present |
| PARTIAL | No wildcard imports added in bulk | 28 wildcard imports remain in pre-existing production files; none were added by the current tracked edits |
| PARTIAL | Validated config, health, metrics, and reindex operation | Config now includes a bounded embedding retry/backoff policy; health, metrics, `SchemaRoutingAdminController` dry-run/reindex, idempotent service rebuild, typed provider-failure mapping, and a 2-test HTTP contract exist. Circuit breaker, query embedding cache, security/concurrent-reindex readiness integration coverage, and some operational metrics remain incomplete |
| NOT DONE | No duplicated alias/default/output definitions across layers | Legacy parameter binders and schema/output compatibility metadata still duplicate contract details |
| DONE | `git diff --check` is clean | Pass, with Git line-ending notices |

### Build and test gates

| Status | Requirement | Evidence / gap |
|---|---|---|
| DONE | Backend clean full test | Final revision `cd backend; $env:DOCKER_HOST='npipe:////./pipe/dockerDesktopLinuxEngine'; .\mvnw.cmd -q clean test` exits 0; 497 tests, 0 failures, 0 errors, 0 skipped across 122 Surefire XML reports. Includes the V6 missing-`schema_versions` repair and legacy-row preservation, V1–V12 bootstrap/upgrade, live PostgreSQL/pgvector integration, V11 typed snapshot identity, V12 ambiguity/reviewer repair, replay mismatch guard, historical definition checksum/retired-topic checks, permit-gated service preview re-execution, bounded assignment run selectors, current binding audit, concrete historical adapter execution, exact BM25/RRF fixtures, strict candidate unit-membership validation, strict reference-output-key validation, direct typed end-condition resolution and vector trim regression, compiled source-key/group projection, native projectile output, post-trim output validation, schema-mode guard, admin reindex HTTP contract, routing-to-persistence, and persisted assignment/snapshot replay. Surefire still logs the known 30-second fork shutdown notice and Hikari teardown warnings after successful tests. |
| DONE | Backend deployable artifact | `cd backend; .\mvnw.cmd -q package -DskipTests` exits 0 and produces `demo-0.0.1-SNAPSHOT.jar`; `jar tf` confirms `BOOT-INF/classes/db/migration/beforeMigrate__repair_v6_missing_schema_versions.sql` is included in the deployable artifact |
| DONE | PostgreSQL/pgvector migration integration | V8–V12 pass against empty and represented-prior fixtures; `FreshSchemaBootstrapMigrationTest` applies full V1–V12 after Hibernate schema creation and baseline 0, including V11 simulation-run identity columns/index and V12 ambiguity/reviewer tables. |
| PARTIAL | Fresh installation and supported upgrade are reproducible without unsafe schema mutation | Testcontainers proves the documented Hibernate-first empty-database sequence, the V7→V12 upgrade, and the narrowly guarded V6 missing-`schema_versions` repair through V7–V12 plus Hibernate validation while preserving legacy data. `DatabaseSchemaModeValidatorTest` rejects Flyway plus Hibernate mutation on normal startup. A direct one-pass Flyway clean bootstrap remains unsupported by the existing V4–V7 history, and backup/restore rehearsal is still open |
| DONE | Full routing → AI fake → canonical runtime → solver → validation → persistence | `SchemaRoutingSimulationPersistenceIntegrationTest` composes schema routing, strict fake extraction and candidate membership, raw-unit normalization, typed execution, reference validation, and captured persisted run snapshot |
| DONE | Frontend architecture/lint/test/build | `npm run check` passes all steps and 23 tests |
| DONE | Scene contract | 182 schemas, 92 explicit scene graphs, 90 series-driven scenes, zero errors |
| DONE | Curriculum/topic coverage does not decrease | 90/90 curriculum rows and 85/85 lessons across 9 topics |
| DONE | No new public dimensional suffix IDs | Current catalog identity fields contain no public `1d`/`2d` suffix |
| PARTIAL | No API/replay/assignment snapshot regression | V11 pins typed identity on newly persisted simulation runs and rejects mismatched replay metadata. `HistoricalRunSnapshotReplayIntegrationTest` covers real PostgreSQL persisted snapshot and assignment save/clear/reload/replay while preserving pinned identity/content. A permit-gated service preview regression is covered; real PostgreSQL full legacy solver re-execution and all output-shape replay coverage remain open |
| PARTIAL | Warnings classified | Deprecated API, Byte Buddy, PostgreSQL/Hikari teardown, Surefire 30-second fork shutdown notice, and Vite chunk-size warnings remain; backend still exits successfully |

## 5. Compatibility, migration behavior, and remaining risks

- Existing persisted schema versions and simulation/assignment snapshots are not rewritten. Current catalog display-text repairs use new schema versions.
- The 28 archived pre-correction versions are exact historical schema data; some legacy display text may remain corrupted in that retired archive to preserve definition checksums. The archive is loaded for pinned replay and is never used to build search documents or AI prompt candidates.
- Missing archive rows are inserted as retired by bootstrap before active catalog entries. Existing rows keep their lifecycle status. Version-pinned schema and solver lookup accepts non-draft retired entries so replay does not require reactivation.
- V8 stores derived search data. V9 adds a nullable solver binding checksum, backfilled only after a source match. V10 pins the search projection version so projection changes cannot reuse old embeddings. V11 adds nullable typed contract identity columns to `simulation_runs`; V12 creates only missing ambiguity/reviewer relations when their Hibernate-owned parent tables exist. New writes populate schema/version, binding and numerical/reference IDs, output-contract version/checksum, and replay validates consistency without rewriting historical rows.
- `beforeMigrate__repair_v6_missing_schema_versions.sql` repairs only the observed state where the latest successful Flyway version is exactly 6 and `schema_versions` is absent. It creates the entity-compatible table before unchanged V7 runs. Existing tables, published rows, other Flyway versions, and Flyway history remain untouched; PostgreSQL/Testcontainers tests cover both the missing-table repair and existing-row preservation.
- New extraction requests use candidate routing. `SimulationService.run()` requires the exact latest approved identity; the dimensional-suffix adapter is only used by the published-version replay resolver. Current execution cannot fall back to raw solver/reference methods. New persisted runs pin their compiled schema/binding/output-contract identity, and replay rejects mismatched metadata. Persisted historical snapshot and assignment replay are covered by real PostgreSQL integration tests; a focused service preview proves historical re-execution is permit-gated, while full PostgreSQL service-level legacy replay remains a gap.
- `SchemaRoutingHealthIndicator` requires the vector extension and a ready index when routing is enabled. Embedding failure does not silently fall back to lexical retrieval.
- Vector search is scoped to the current approved `schemaId@version` snapshot. Both pgvector integration tests pass against PostgreSQL 16 and cover an older approved embedding that would otherwise occupy a top-K slot.
- Schema lifecycle and topic enable/disable changes refresh the search index after commit. A failed rebuild leaves routing unready, and health checks index identity against current approved schemas. Live PostgreSQL coverage now exercises approved-version changes, retired/draft/disabled exclusion, projection-version freshness, and fail-closed embedding failure. Provider circuit breaker, query embedding cache, and a full admin reindex endpoint integration test remain incomplete. Embedding transport retries are bounded by the configured attempt/backoff window and do not create a silent retrieval fallback.
- A direct, one-pass Flyway clean bootstrap remains unsupported because the existing V4–V7 migrations expect Hibernate-created base tables. The verified Hibernate-first, baseline-0 fresh-install sequence is documented and covered by `FreshSchemaBootstrapMigrationTest`. It is restricted to an empty database; do not enable baseline-on-migrate for an existing or partially initialized database.
- All 74 current routed schema identities use typed modules. Legacy JSON-based solver/reference implementations and their 59 parameter binders remain only under explicit compatibility packages; there is no complete all-model independent oracle matrix, fully typed solver output integration, or per-model mutation suite.

Until these gaps are closed and all blocked gates are run successfully, do not describe the backend as production ready.
