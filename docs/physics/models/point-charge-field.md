# `point_charge_field`

For an isolated point charge, `E=k|q|/r²` and `V=kq/r`, with
`k=8.9875517923×10⁹ N·m²/C²`. The output is a scalar magnitude/potential
probe; it is not a claim to render every vector line in space.
