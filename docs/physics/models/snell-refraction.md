# `snell_refraction`

For a planar interface, `n₁ sin θ₁ = n₂ sin θ₂`. Angles are measured from the
normal in radians. When `n₁ sin θ₁ > n₂`, the reference marks total internal
reflection and does not invent a refracted ray.
