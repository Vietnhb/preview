# `ac_rlc_circuit`

For a series RMS AC circuit, `X_L=ωL`, `X_C=1/(ωC)`,
`Z=√(R²+(X_L−X_C)²)`, `I=V_rms/Z`, `cosφ=R/Z`, and
`P=I²R`. The model is a phasor-domain lesson; it does not animate a
second-order transient while claiming to be the steady-state AC solution.
