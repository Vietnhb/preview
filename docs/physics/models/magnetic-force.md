# `magnetic_force`

The Lorentz magnetic-force magnitude is `F=|q|vB sin θ`, where θ is the angle
between velocity and magnetic field. Parallel motion gives zero force and
perpendicular motion gives the maximum magnitude. This model intentionally
returns the scalar magnitude; it does not claim a vector direction or a full
magnetic-field map.
