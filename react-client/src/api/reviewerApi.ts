/**
 * API endpoints for reviewer operations
 * Handles review queue, topic schemas, solvers, and benchmarks
 * (Currently placeholder - endpoints will be added as reviewer features are implemented)
 */

// Placeholder for future reviewer endpoints
// Examples:
// export const reviewQueue = () => axiosClient.get("/reviewer/queue").then(r => r.data);
// export const topicSchemas = () => axiosClient.get("/reviewer/schemas").then(r => r.data);
// export const solverBenchmarks = () => axiosClient.get("/reviewer/benchmarks").then(r => r.data);
