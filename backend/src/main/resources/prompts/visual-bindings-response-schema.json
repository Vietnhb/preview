{
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "visualBindings": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "targetId": { "type": "string" },
          "entityId": { "type": ["string", "null"] },
          "assetId": { "type": ["string", "null"] },
          "match": { "type": "string", "enum": ["EXACT", "SUBSTITUTE", "UNSUPPORTED", "OMITTED"] },
          "visualDifference": { "type": ["string", "null"] }
        },
        "required": ["targetId", "entityId", "assetId", "match", "visualDifference"]
      }
    }
  },
  "required": ["visualBindings"]
}
